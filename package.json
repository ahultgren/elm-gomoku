{
  "name": "elm-gomoku",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "http-server -p 3023",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "elm-make --warn src/Main.elm --output public/dist/elm.js",
    "dev-build": "npm run build -s",
    "dev": "nodemon -e 'elm' -x npm run dev-build -s"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.9.0",
    "nodemon": "^1.11.0"
  }
}
